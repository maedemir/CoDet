function check_ct()
{
    var file = document.getElementById('file_ct').value
    var type = document.getElementById('inputType_ct').value
    
    if(!file || type ==="0"){
        alert ("You should select file and model!")
    }
}

function check_x()
{
    var file = document.getElementById('file_x').value
    var type = document.getElementById('inputType_x').value
    
    if(!file || type ==="0"){
        alert ("You should select file and model!")
    }
}

